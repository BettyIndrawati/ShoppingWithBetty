
@model ShoppingWithBetty.DataAccess.Repositories.ViewModels.ProductVM

@{
    ViewData["Title"] = "Index";
}
<div class="container p-4">
    <div class="row p-3">
        <div class="col-6">
            <h2 class="text-primary">List of Products</h2>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-action="CreateUpdate" asp-controller="Product"><i class="bi bi-plus-circle"></i>&nbsp; Create New Product</a>
        </div>
    </div>
</div>
<table id="myTable" class="table table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Product Name
            </th>
            <th>
                Product Description
            </th>
            <th>
                Price
            </th>
            <th>
                Catagory
            </th>
            <th>
                Action
            </th>
        </tr>

    </thead>
    @*<tbody>
        @foreach (var item in Model.Catagories)
        {
            <tr>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.DisplayOrder
                </td>
                <td role="group">
                    <li>
                        <a asp-action="CreateUpdate" asp-controller="catagory" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Update Detail Catagory</a>
                    </li>
                    <li>
                        <a asp-action="Delete" asp-controller="catagory" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete Catagory</a>
                    </li>
                </td>
            </tr>
        }
    </tbody>*@
</table>
@section scripts{
    <script src="~/js/Product.js"></script>
}